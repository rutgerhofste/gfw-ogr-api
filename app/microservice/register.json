{
    "id": "#(service.id)",
    "name": "#(service.name)",
    "urls": [{
        "url": "/ogr/convert",
        "method": "POST",
        "endpoints": [{
            "method": "POST",
            "path": "/api/v1/ogr/convert"
        }]
    }]
}
