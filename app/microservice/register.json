{
    "id": "#(service.id)",
    "name": "#(service.name)",
    "tags": ["gfw"],
    "urls": [{
        "url": "/ogr/convert",
        "method": "POST",
        "endpoints": [{
            "method": "POST",
            "path": "/api/v1/ogr/convert"
        }]
    }]
}
