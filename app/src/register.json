{
    "id": "#(service.id)",
    "name": "#(service.name)",
    "urls": [{
        "url": "/convert",
        "method": "POST",
        "endpoints": [{
            "method": "POST",
            "baseUrl": "#(service.uri)",
            "path": "/api/v1/ogr/convert"
        }]
    }]
}
