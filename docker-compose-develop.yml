develop:
  extends:
    file: base.yml
    service: base
  ports:
    - "35730:35729"
  container_name: gfw-ogr-api-develop
  environment:
    NODE_ENV: dev
  command: develop
  volumes:
    - ./app:/opt/gfw-ogr-api/app
